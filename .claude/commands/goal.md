---
description: "Set or clear the automode goal (shown during idle cycles instead of restless message)"
argument-hint: "Goal description, or 'clear' to remove"
allowed-tools: ["Bash(automode goal:*)"]
---

```!
automode goal "$ARGUMENTS"
```
